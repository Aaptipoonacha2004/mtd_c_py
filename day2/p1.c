//garbage collector- frame once used by a function, it is cleared.
// functional programming- procedural + oop
// arithmetic operators: + - * / %- infix notation: operator between the operands -binary operators- i/p and o/p are numbers- data 
// &- and completes all possible cases &&- short circuited and: will not continue if one of them fails
// IIly or
#include <stdio.h>
#include <math.h>

int main() {
    int input_number;

    puts("Enter a number to check for perfect square: ");
    scanf("%d", &input_number);

     int calculated_square_root = sqrt(input_number);

    // if (calculated_square_root * calculated_square_root == input_number) {
    //     puts("Yes, it is a perfect square.");
    if (type(calculated_square_root) == type(5)){
         printf("working");
    }
    return 0;
    }

  
